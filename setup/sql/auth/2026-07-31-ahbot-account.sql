-- Акаунт, който притежава обявите на mod-ah-bot.
--
-- Модулът иска акаунт и герой, на чието име да стоят обявите
-- (AuctionHouseBot.Account / AuctionHouseBot.GUID в mod_ahbot.conf).
--
-- Защо отделен акаунт, а не съществуващ бот:
--   Парите от продажби идват по поща на собственика. Ако това е жив playerbot,
--   пощата отива при герой, който сам чете поща - и командата
--   'playerbots rndbot reset' би изтрила героя, а с него и всички обяви.
--
-- Идентификаторът е ЗАКОВАН на 900, а не автоматичен, за да е един и същ на
-- всяка машина - конфигурацията сочи към него по число.
--
-- В акаунта НЕ може да се влезе: salt и verifier са нули, така че SRP6
-- проверката не може да съвпадне с никаква парола. Това е нарочно.

DELETE FROM `account` WHERE `id` = 900;
INSERT INTO `account` (`id`, `username`, `salt`, `verifier`, `email`, `expansion`) VALUES
  (900, 'AHBOT', REPEAT(0x00, 32), REPEAT(0x00, 32), '', 2);

-- Без GM права. Изрично, защото акаунт без ред тук е обикновен играч, но по-добре
-- да е записано, отколкото да се подразбира.
DELETE FROM `account_access` WHERE `id` = 900;
